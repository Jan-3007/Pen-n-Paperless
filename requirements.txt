# requirements.txt file
## Minimal dependencies for this Flask + SQLAlchemy app.
# Use the virtualenv you created before installing:
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install -r requirements.txt

# Core web framework
Flask>=2.3.0,<3

# Database and ORM
Flask-SQLAlchemy>=2.5.1,<3
SQLAlchemy>=1.4,<2

# Load .env files into environment (optional but handy in development)
python-dotenv>=0.21.0

# PostgreSQL driver (only required if you use Postgres in production)
# psycopg2-binary>=2.9

# Testing (optional)
pytest>=7.0

# HTTP client (optional; add back only if your app makes outbound requests)
# requests>=2.28

# Remove heavy data-science packages (numpy/pandas) unless you actually need them.

# Image processing for avatar uploads
Pillow>=9.0.0
